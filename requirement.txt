# Tested with Python 3.11. Install torch/torchvision for your CUDA version.
# Example: https://pytorch.org/get-started/locally/
torch>=2.0
torchvision>=0.15

# Core training and inference dependencies.
numpy>=1.24,<2.0
opencv-python-headless>=4.8
Pillow>=10.0
PyYAML>=6.0
tqdm>=4.66
lmdb>=1.4
scipy>=1.10
scikit-image>=0.21
timm>=1.0
torchinfo>=1.8
tensorboard>=2.14
requests>=2.31
setuptools>=65

# Losses and validation metrics.
pyiqa==0.1.13
lpips>=0.1.4
pytorch-msssim>=1.0.0
ftfy>=6.1
regex>=2023.0

# Optional utilities:
# wandb>=0.16          # only needed when logger.wandb.project is set
#
# Muon note:
# The current train configs use optim_g.type: Muon. The existing environment
# has a standalone site-packages/muon.py, but no pip package metadata for it.
# Vendor/install that module before Muon training, or switch optim_g.type to Adam.
